.headline-component {
  background-image: linear-gradient(to top, #3e3e59, #454573);
  color: #ffffff80;
}

.headline-container {
  display: flex;
  padding: 16px 0;
  align-items: center;
  max-width: $bp-desktop;
  margin: 0 auto;
}

.btns {
  margin-left: auto;
}

.btn {
  color: #ffffffb3;
  text-decoration: underline;
}

.title {
  margin-left: 30px;
}