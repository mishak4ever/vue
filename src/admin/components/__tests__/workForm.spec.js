import workForm from "../workForm/workForm.vue";
import { mount } from "@vue/test-utils";

it("проверяет работу валидатора формы", async () => {
  // const wrapper = mount(workForm);

  // expect(wrapper.element).toMatchSnapshot();
  // await wrapper.find("#save_btn").trigger("click");

  // expect(wrapper.find("#title_input").props("errorMessage").isEmpty).toBe(
  //   "false"
  // );
});
